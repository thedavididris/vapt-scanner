# Supabase client
supabase==2.3.1

# Security scanning tools Python wrappers
python-nmap==0.7.1

# HTTP clients (httpx version compatible with supabase)
requests==2.31.0
httpx==0.25.2

# Data validation and parsing
pydantic==2.5.0

# Environment management
python-dotenv==1.0.0
